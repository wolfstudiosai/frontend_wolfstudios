import { config } from '/src/config';
import { Typography } from '@mui/material';

export const metadata = { title: `Dashboard | Content | ${config.site.name}` };

export default function Page() {
  return (
    <>
    <Typography>
        Add Content page will Coming Soon...
    </Typography>
    </>
  );
}
