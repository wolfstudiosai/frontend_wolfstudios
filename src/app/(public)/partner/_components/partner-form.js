import { Box } from "@mui/material"

export const PartnerForm = (
    data,
    errors,
    onSubmit,
    onChange,
    onSetFile,
    onDeleteThumbnail,
    setFieldValue
) => {
    return (
        <Box>Partner form</Box>
    )
}