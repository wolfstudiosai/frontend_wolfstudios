import { config } from '@/config';
import { Typography } from '@mui/material';

export const metadata = { title: `Dashboard | Content | ${config.site.name}` };

export default function Page() {
  return (
    <>
    <Typography>
        Content page will Coming Soon...
    </Typography>
    </>
  );
}
