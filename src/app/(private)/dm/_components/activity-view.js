import Grid from "@mui/material/Grid2";

export const ActivityView = () => {
    return (
        <Grid container sx={{ border: '1px solid', borderColor: 'divider', borderRadius: 1, width: '100%', p: 1 }}>
            This is the activity container
        </Grid>
    )
}