import { Box, Typography } from '@mui/material';

export const CampaignPreview = ({ data }) => {
    return (
        <Box>
            <Typography variant="body2">Campaign</Typography>
        </Box>
    );
};