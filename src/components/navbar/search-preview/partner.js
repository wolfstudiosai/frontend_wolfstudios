import { Box, Typography } from '@mui/material';

export const PartnerPreview = ({ data }) => {
    return (
        <Box>
            <Typography variant="body2">Partner</Typography>
        </Box>
    );
};