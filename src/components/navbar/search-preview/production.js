import { Box, Typography } from '@mui/material';

export const ProductionPreview = ({ data }) => {
    return (
        <Box>
            <Typography variant="body2">Production</Typography>
        </Box>
    );
};